class Solution {
public:
   string toLowerCase(string str) {
       int size = str.size();
        for(int i = 0; i < size ; i++){
            if((str[i] >= 'A') && (str[i] <= 'Z')){
                str[i] += 32;
            }
        }
        
        return(str);
    }
};
